import * as React from 'react'
import './Cards.css'

export default function Card ()({title, copyText }) => {
    return (
        <section className="Card">
          <button className={isBookmarked
             ? 'Card__bookmark" aria-label="Bookmark"></button>
          <h2 className="Card__title">{title}</h2>
            <Bookmark active={bookmark}
            
            <p className="Card__text">Lorem ipsum dolor sit amet.</p>
        </section>
    )
}